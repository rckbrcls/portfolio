# Troubleshooting

## Blog post does not appear

Check the MDX file under `content/blog/` and the parsing helpers in `src/lib/`.

## Project information is outdated

Update the structured data under `data/` and keep claims aligned with the live project repositories.

## Visual changes feel off

Review `DESIGN.md` before changing typography, spacing, color, or motion.
